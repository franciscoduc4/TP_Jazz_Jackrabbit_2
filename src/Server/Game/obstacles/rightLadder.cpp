#include "rightLadder.h"

RightLadder::RightLadder(GameMap& gameMap, Vector<uint32_t> position)
    : Obstacle(gameMap, position) {}

void RightLadder::update(double deltaTime) {
    // Implementar lógica específica para RightLadder
}
