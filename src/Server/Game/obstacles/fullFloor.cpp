#include "fullFloor.h"

FullFloor::FullFloor(GameMap& gameMap, Vector<uint32_t> position)
    : Obstacle(gameMap, position) {}

void FullFloor::update(double deltaTime) {
    // Implementar lógica específica para FullFloor
}
