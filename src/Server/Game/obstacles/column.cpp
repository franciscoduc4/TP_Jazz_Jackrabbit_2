#include "column.h"

Column::Column(GameMap& gameMap, Vector<uint32_t> position)
    : Obstacle(gameMap, position) {}

void Column::update(double deltaTime) {
    // Implementar lógica específica para Column
}
