#include "leftLadder.h"

LeftLadder::LeftLadder(GameMap& gameMap, Vector<uint32_t> position)
    : Obstacle(gameMap, position) {}

void LeftLadder::update(double deltaTime) {
    // Implementar lógica específica para LeftLadder
}
